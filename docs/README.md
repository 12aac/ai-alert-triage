Place dashboard screenshots here:

- `screenshot-overview.png` — Overview tab after a run on UNSW-NB15 data
  (metric cards + decision donut visible).
- `screenshot-scores.png` — Anomaly scores tab showing the routing bands.

Capture at ~1280px wide. On Ubuntu: Shift+PrtScn to select a region.
